export interface RideScheduleInterface {
    StartTime?: string;        // เวลาเริ่มต้น
    EndTime?: string;          // เวลาสิ้นสุด
    Capacity?: number;         // ความจุของรอบนี้
    RideID?: number;           // รหัสของเครื่องเล่น
   
}
